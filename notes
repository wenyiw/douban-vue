Day 1
  Step 1
    Link vue object to #app
      App.vue and main.js
  Step 2
    header guides and routers
      App.vue: router-view
      main.js: import router
      HeaderGuide.vue: router-links
      router/index.js: define routers from pages
      pages/: code (placeholder for now) for each router-view
  Step 3
    pages/Home.vue
    components/Activities.vue
      script
        import mapstate, components, etc.
        export default
          name,
          components,
          computed,
            map state
          created
            get activities from the store by calling actions
            note: ad-blocker may block calls (net::ERR_BLOCKED_BY_CLIENT)
    vuex
      main.js: import vuex
      store/index.js: export modules
      store/modules: 
        .js: define states, mutations, actions
        mutations directly modify states, actions call mutations
Day 2
  Step 4
    implement details of each activity from the home view
    pages/Activity.vue
    in pages/Activities.vue
      router link with name activity
    in router/index.js
      import pages/Activity, add a route with the name and the component 
  Step 5
    pages/Books.vue
      get books from the store
    vuex: same as in step 3

Day 3
  Step 6
    pages/Movies.vue
      get movies from the store
    vuex: same as in step 3
  Step 7
    pages/Broadcasts.vue
      login/signup

Day 4
  Step 8
    movie details

Day 5 
  Step 9
    Broadcasts: simply reuse card and user components
  Step 10
    pages/Search.vue












